<%= form_for([@post, @post.comments.build ]) do |form| %>
  <p>
    <%= form.label :user %><br>
    
  </p>
   
  <p>
    <%= form.label :body %><br>
    <%= form.text_area :body %>
  </p>
  <p>
    <%= form.submit   %>
  </p>
<% end %>
